# Copy to keystore.properties and fill in the real values.
# keystore.properties is gitignored — never commit it.
#
# storeFile is resolved relative to the repo root.
storeFile=BirthdayReminderKeyStore.jks
storePassword=
keyAlias=
keyPassword=
